const fs = require("fs");

module.exports = function(app) {
    let notes = reuire("../db/db.json")

    // Set up the /api/notes get route

    // Set up the /api/notes post route

    // Retrieves a note with specific id

    // Deletes a note with a specific id


    
}